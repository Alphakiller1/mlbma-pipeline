# Credentials
.env

# Data files (too large for GitHub, regenerated by pipeline)
data/*
!data/public/
!data/public/**

# Python cache
__pycache__/
*.pyc
*.pyo

# Packaging build artifact (pip install -e .)
*.egg-info/

# Virtual environment
crawl_env/

# VS Code
.vscode/
google_credentials.json

pipeline_log.txt
pipeline_smoke_log.txt
pipeline_*.log
outputs/instagram_post_manifest.json
cursor

# Local Cloudflare deploy build (scripts/deploy_cloudflare.py)
_site/

# generated daily social posting queue
outputs/social_queue/

# Local design exploration — not for the public production domain
dashboard/mockups/

# Build / run artifacts — never belong in the public Pages repo
dashboard/_mc_live.png
node_modules/
tmp_*
pipeline_run_*.log
outputs/reading_list/
