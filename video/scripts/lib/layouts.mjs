/** Layout names, mirrored from src/edit/frames.ts (LAYOUT_MODES) for the Node scripts. */
export const LAYOUT_MODES_JS = ["bubble", "split", "full", "host"];
/** Mirrors STAGE_SIZES in frames.ts. */
export const STAGE_SIZES_JS = ["full", "compact", "small"];
/** Small graphics that can sit over any stage graphic (booth keys B, N, K). */
export const OVERLAYS_JS = { bug: "CornerBug", name: "LowerThird", ticker: "Ticker" };
